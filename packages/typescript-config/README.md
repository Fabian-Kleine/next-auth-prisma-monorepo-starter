# `@repo/typescript-config`

Shared TypeScript configurations for the monorepo.

- `base.json` — base config for all packages
- `nextjs.json` — for the Next.js app
- `react-library.json` — for React library packages
